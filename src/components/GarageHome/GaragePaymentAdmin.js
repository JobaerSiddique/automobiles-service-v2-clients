import React, { useContext, useEffect } from 'react';
import { AuthContext } from '../../Contexts/AuthContext/AuthProvider';

const GaragePaymentAdmin = () => {
    const {user}=useContext(AuthContext)
    
    
    return (
        <div>
            
        </div>
    );
};

export default GaragePaymentAdmin;